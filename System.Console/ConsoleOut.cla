class ConsoleOut : Out
{
    field probate ConsoleIntern Intern { get { return data; } set { data : value; } }

    field probate Int Stream { get { return data; } set { data : value; } }

    maide prudate Bool Write(var String o)
    {
        this.Intern.Write(this.Stream, o);
        return true;
    }
}